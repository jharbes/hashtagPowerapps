Nome do usuario:

User().FullName


Mudar de tela:

Navigate(nome_da_tela)


Atualiza lista de sharepoint de dentro do aplicativo PowerApps:

Patch(listaTeste,Defaults(listaTeste),
{
    Nome: txtNome.Text,
    Cargo: txtCargo.Text,
    Salario: Value(txtSalario.Text)
})



Atualiza lista de sharepoint de dentro do aplicativo PowerApps, com mensagem de sucesso no final,
também funciona como criação de linha (novo item) com a mesma sintaxe:

Patch(listaTeste,Defaults(listaTeste),
{
    Nome: txtNome.Text,
    Cargo: txtCargo.Text,
    Salario: Value(txtSalario.Text)
});

Notify("Funcionário cadastrado com sucesso!")


